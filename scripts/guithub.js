// GuitHub, a guitar scale helper web app
// It tells you what notes to play! (sort of)

// playing with Snap SVG for a bit
var s = Snap("#fretboard");

// draw a circle, see if it works
var circle = s.circle(150, 150, 10);